{
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "jest",
    "test:watch": "npm test -- --watch",
    "test:browserstack": "playwright test --config=browserstack-demo/playwright.config.js browserstack-demo/browserstack-playwright.spec.js",
    "build": "turbo run build",
    "tsc": "turbo tsc",
    "changeset": "changeset",
    "changeset:version": "changeset version && npm install",
    "changeset:publish": "npm run build && changeset publish"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.4.7",
    "@changesets/cli": "^2.25.2",
    "@playwright/test": "^1.40.0",
    "browserstack-local": "^1.2.8",
    "playwright": "^1.40.0",
    "serve-handler": "^6.1.5",
    "turbo": "^1.6.3"
  },
  "jest": {
    "projects": [
      "<rootDir>/packages/*/jest.config.cjs"
    ]
  }
}
